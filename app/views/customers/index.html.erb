<div class="col-sm-12">
  <div class="white-box">
      <h3 class="box-title m-b-0 text-center">CUSTOMERS</h3>
      <p class="text-muted m-b-30"></p>
      <div class="table-responsive custom_datatable">
        <table id="companies_datatable" class="display nowrap" cellspacing="0" width="100%">
          <thead>
            <tr>
                <th>Name</th>
                <th>Fax</th>
                <th>Edit</th>
                <th> Destroy</th>
            </tr>
          </thead>
          <tbody>
            <% @customers.each do |contact| %>
              <tr>
                  <td><%= link_to contact.name, contact %></td>
                  <td><%= contact.fax %></td>
                  <td><%= link_to 'Edit', edit_contact_path(contact) %></td>
                  <td><%= link_to 'Destroy', contact, method: :delete, data: {confirm: 'Are yoy sure'} %>
              </tr>
            <%end%>
          </tbody>
        </table>
      </div>
  </div>
</div>
<script>
  datatable('companies_datatable', 1, [2,3], 20, [0,1])
</script>
